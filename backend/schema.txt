Based on the migrations I can see, here's the current database schema:
users table:
id (INTEGER, primary key, auto-increment)
username (STRING, unique, not null)
email (STRING, unique, not null)
password (STRING, not null)
role (ENUM: 'user', 'admin', 'manager', not null, default: 'user')
googleId (STRING, unique, nullable)
githubId (STRING, unique, nullable)
createdat (DATE, not null)
updatedat (DATE, not null)
ideas table:
id (INTEGER, primary key, auto-increment)
title (STRING, not null)
description (TEXT, not null)
submittedby (INTEGER, foreign key to users.id, nullable)
status (ENUM: 'pending', 'approved', 'rejected', 'in_progress', 'completed', not null, default: 'pending')
documentUrl (STRING(255), nullable)
createdat (DATE, not null)
updatedat (DATE, not null)
votes table:
id (INTEGER, primary key, auto-increment)
ideaid (INTEGER, foreign key to ideas.id, not null)
userid (INTEGER, foreign key to users.id, not null)
createdat (DATE, not null)
updatedat (DATE, not null)
Unique constraint on (ideaid, userid)
projects table (based on the model):
id (INTEGER, primary key, auto-increment)
title (STRING, not null)
description (TEXT, not null)
status (ENUM: 'planning', 'in_progress', 'completed', not null, default: 'planning')
managerid (INTEGER, foreign key to users.id, nullable)
ideaid (INTEGER, foreign key to ideas.id, nullable)
createdat (DATE, not null)
updatedat (DATE, not null)
tasks table (based on the model):
id (INTEGER, primary key, auto-increment)
title (STRING, not null)
description (TEXT, not null)
status (ENUM: 'todo', 'in_progress', 'completed', not null, default: 'todo')
priority (ENUM: 'low', 'medium', 'high', not null, default: 'medium')
duedate (DATE, nullable)
assignedto (INTEGER, foreign key to users.id, nullable)
projectid (INTEGER, foreign key to projects.id, nullable)
createdat (DATE, not null)
updatedat (DATE, not null)
All tables use snake_case for column names and have createdat and updatedat timestamps. The associations between tables are:
Users can submit ideas (one-to-many)
Users can vote on ideas (many-to-many through votes table)
Users can be assigned tasks (one-to-many)
Users can manage projects (one-to-many)
Projects can have tasks (one-to-many)
Projects can be based on ideas (one-to-one)
Would you like me to check any specific part of the schema in more detail?